# Portfolio — Telegram Bot + Static Site

Портфолио с каталогом фигурок, интегрировано с Telegram ботом.

## Быстрый запуск локально

### 1. Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

pip install -r app/requirements.txt
```

### 2. Запуск веб-приложения (FastAPI)

```bash
cd app
python main.py
```

Откройте http://127.0.0.1:8000

### 3. Запуск Telegram-бота

В отдельном терминале:

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
export WEBAPP_URL="https://ваш-домен.com"  # или http://127.0.0.1:8000 для тестов
python app/bot.py
```

### 4. Генерация статического сайта для GitHub Pages

```bash
python app/build_static.py
# Результат в папке `docs/`
```

---

## Запуск 24/7 на Render.com (рекомендуется для обучения)

### Шаг 1: Подготовка репозитория

Убедитесь, что все файлы закоммичены и запушены в GitHub:

```bash
git add .
git commit -m "Add Dockerfile and render.yaml for deployment"
git push origin main
```

### Шаг 2: Создание аккаунта на Render.com

1. Перейдите на https://render.com
2. Создайте аккаунт (можно через GitHub)
3. Подключите ваш GitHub репозиторий

### Шаг 3: Развёртка сервиса

1. На Render нажмите **New +** → **Web Service**
2. Выберите ваш репозиторий `Portfolio`
3. Заполните:
   - **Name**: `portfolio-bot`
   - **Runtime**: `Docker`
   - **Build Command**: оставьте пусто (будет использован Dockerfile)
   - **Start Command**: `python app/bot.py`
4. В разделе **Environment** добавьте переменные:
   - `TELEGRAM_BOT_TOKEN` = ваш токен от @BotFather
   - `WEBAPP_URL` = URL вашего сайта (получится после деплоя Pages)

### Шаг 4: Деплой Pages (статический сайт)

1. На GitHub зайдите в **Settings** → **Pages**
2. Выберите:
   - **Source**: Branch: `main`, Folder: `/docs`
3. GitHub автоматически опубликует сайт, URL будет вида: `https://<username>.github.io/<repo>/`

### Шаг 5: Обновление WEBAPP_URL

После публикации Pages:

1. На Render обновите переменную `WEBAPP_URL` на ваш GitHub Pages URL
2. Сервис автоматически перезагрузится

---

## Telegram бот — как использовать

1. Найдите вашего бота в Telegram (поиск `@<ваш_username_бота>`)
2. Нажмите `/start`
3. Появится кнопка «🎨 Открыть каталог» → нажмите, откроется ваш сайт

---

## Файловая структура

```
Portfolio/
├── app/
│   ├── main.py              # FastAPI приложение
│   ├── bot.py               # Telegram бот
│   ├── data.py              # Данные (коллекции, фигурки)
│   ├── build_static.py      # Генератор статических страниц
│   ├── templates/           # Jinja2 шаблоны
│   └── requirements.txt      # Python зависимости
├── static/                  # Статические файлы (CSS, изображения)
├── docs/                    # Сгенерированный статический сайт (GitHub Pages)
├── Dockerfile               # Docker конфигурация
├── render.yaml              # Render конфигурация
└── README.md                # Этот файл
```

---

## Переменные окружения

### Для локального запуска

Создайте файл `.env`:

```bash
TELEGRAM_BOT_TOKEN=8579039126:AAFduTOX1YZKw0Y41T-rCWEuygLC_cVdSMw
WEBAPP_URL=http://127.0.0.1:8000
```

Потом загрузите в Python:

```python
from dotenv import load_dotenv
import os

load_dotenv()
TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
```

### Для Render.com

Добавьте в настройки сервиса на Render в разделе **Environment**.

---

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot)
- [Render.com Docs](https://render.com/docs)
- [FastAPI Docs](https://fastapi.tiangolo.com/)
- [GitHub Pages Docs](https://docs.github.com/en/pages)

---

## Проблемы и решения

### Бот не запускается

1. Проверьте токен (от @BotFather)
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлена как переменная окружения
3. Посмотрите логи на Render: **Logs** (внизу страницы сервиса)

### Сайт не открывается из бота

1. Проверьте `WEBAPP_URL`
2. Убедитесь, что URL заканчивается на `/` если это корень сайта
3. Обновите сервис на Render (нажмите **Manual Deploy**)

### GitHub Pages не обновляется

1. Убедитесь, что `docs/` в репозитории и зафиксирована в git
2. Зайдите в **Settings** → **Pages** и проверьте ветку и папку
3. Если всё правильно, зайдите на URL Pages через 1-2 минуты

---

## Дальнейшее развитие

- Добавьте оплату (Stripe, YooKassa) для покупок через бота
- Интегрируйте базу данных (PostgreSQL на Render)
- Добавьте админ-панель для управления каталогом
- Настройте уведомления в Telegram при заказах
- Добавьте поддержку изображений высокого разрешения

---

**Автор**: Portfolio Team  
**Лицензия**: MIT  
**Дата**: 2026
